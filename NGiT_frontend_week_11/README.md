# Explain the difference between == and === in JavaScript logic

== (Loose equality — "kinda equal")
This checks value only, and JavaScript is allowed to convert types automatically before comparing.
"Are these the same VALUE? I don't care what TYPE they are"

=== (Strict equality — "exactly equal")
This checks value AND data type.
"Are these the same VALUE AND the same TYPE?"